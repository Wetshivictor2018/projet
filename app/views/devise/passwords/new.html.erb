
<div class="image image-overlay" style="background-image: url(../../assets/cooodit/cover-test.jpeg);"></div>
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="viewport">
    
    <div class="container">
      <div class="row justify-content-center align-items-center vh-100">
        <div class="col-md-6 col-lg-5">
          <div class="accordion-group accordion-group-portal" data-accordion-group>
            <div class="accordion open" data-accordion>
              <div class="accordion-control" data-control>
                <h5>Mot de passe oublié ?</h5>
              </div>
              <div class="accordion-content" data-content>
                <div class="accordion-content-wrapper">
                  <form>
                    <div class="form-group">
                      <label for="exampleFormControlInput1">Votre adresse email</label>
                      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
                      
                    </div>

                    
                    <%= f.submit "Envoyer", class: "btn btn-primary btn-block" %><br/>
                    <%= link_to "Créer un compte", new_registration_path(resource_name) %> ou
                    <%= link_to "Se connecter", new_session_path(resource_name) %></p><br/>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
